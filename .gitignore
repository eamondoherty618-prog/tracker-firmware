# PlatformIO / build
.pio/
.vscode/
compile_commands.json
*.DS_Store

# Stray build/runtime artifacts (kept out even though the prototype is gone)
.next/
data/*.jsonl

# KiCad local/session artifacts
*.kicad_prl
*.lck
*-backups/

# Personal/other-project files that share this folder but aren't firmware
*.skp
*.zip
fiber_laser_*
prismatic_powder_layer_notes.html
rc2_powder_gun_quick_settings.html
serve_skp_cors.py
vintage-moto-ct-logo-options/
vintage-moto-ct-logo-options-clean/
assets/

# Editor local-history artifacts
.history/
**/.history/
